#!/bin/bash
execute_process(
    COMMAND find . -name CMakeFiles -prune -print -exec rm -rf {} \;
    COMMAND find . -name Makefile -prune -print -exec rm -rf {} \;
    COMMAND rm -rf CMakeCache.txt cmake_uninstall.cmake
)
